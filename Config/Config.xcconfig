BUNDLE_IDENTIFIER_PREFIX = com.ricciadams.opensource

CODE_SIGN_STYLE = Automatic

#include? "../Private/Private.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER_PREFIX).DesktopShield
MARKETING_VERSION = 1.0
